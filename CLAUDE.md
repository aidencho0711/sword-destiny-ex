# CLAUDE.md

Sword Destiny - ex 프로젝트 작업 지침입니다.

## 프로젝트 개요

검 뽑기(가챠) 웹게임. **빌드 과정이 없는 정적 사이트**입니다.
번들러, 프레임워크, npm 의존성이 전혀 없습니다. 파일을 고치고 새로고침하면 끝입니다.

- 47종의 검, 14등급 (평범 → 운명, 1/1 → 1/2억)
- 상위 등급에는 전용 컷신(SVG + CSS 애니메이션 + WebAudio 합성음)
- 상점, 도감, 도전과제 트리, 환생, 기기 내 계정 시스템
- PWA (오프라인 동작, 홈 화면 설치)

## 파일 구조

```
index.html              마크업 셸 (CSS/JS는 외부 참조)
css/game.css            전체 스타일
js/01-data-rarity.js    등급 테이블, 컷신 타이밍 상수
js/02-data-swords.js    검 47종 데이터, 장착 고유 효과(FX)
js/03-data-economy.js   상점(UPGRADES/POTIONS), 환생, 도전과제(ACH)
js/04-state.js          전역 상태 S, 저장/로드, 품질 설정, 계정
js/05-sword-svg.js      검 SVG 생성기
js/06-audio.js          WebAudio 합성 (컷신별 전용 사운드)
js/07-roll.js           뽑기 확률 판정
js/08-cutscenes.js      컷신 연출 (테마별 DOM + 키프레임 생성)
js/09-render.js         화면 렌더링 (HUD/상점/도감/설정/개발자메뉴)
js/10-events.js         이벤트 바인딩
js/11-boot.js           모바일 환경 처리, 로그인, 부팅
sw.js                   서비스 워커
manifest.webmanifest    PWA 매니페스트
icon*.png / icon.svg    앱 아이콘
```

## 반드시 지켜야 할 것

### 1. 스크립트는 classic script다 (module 아님)

`js/*.js`는 `<script src>`로 순서대로 로드되며 **전역 스코프를 공유**합니다.
`import`/`export`가 없고, 파일 간 참조는 전역 변수로 이루어집니다.

- **로드 순서를 바꾸지 마세요.** 파일명 앞의 번호가 곧 실행 순서입니다.
- 새 파일을 추가하면 `index.html`의 `<script>` 목록과 `sw.js`의 `ASSETS`에 **둘 다** 등록해야 합니다.
- ES module로 전환하려면 모든 상호 참조에 import/export를 달아야 합니다. 큰 작업이므로 요청받았을 때만 하세요.

### 2. sw.js 캐시 버전을 올려야 배포가 반영된다

파일을 하나라도 고쳤으면 `sw.js`의 `const CACHE = "sworddestiny-vNN"` 숫자를 올리세요.
올리지 않으면 이미 방문한 기기는 옛 캐시를 계속 씁니다. **가장 흔한 실수입니다.**

### 3. CSS 애니메이션에서 transform을 덮어쓰지 말 것

`@keyframes capIn`은 `transform: none`을 포함합니다.
SVG의 `transform="translate(...)"` 속성이나 인라인 회전을 가진 요소에 `capIn`을 쓰면
**배치가 통째로 무시되어 요소가 원점으로 끌려갑니다.** 이 버그를 두 번 겪었습니다.

투명도만 바꾸려면 `softIn`을 쓰세요.

```css
@keyframes softIn{from{opacity:0}to{opacity:1}}
```

### 4. HTML 속성에 JSON.stringify를 쓰지 말 것

`data-t=${JSON.stringify(text)}`는 따옴표를 `\"`로 이스케이프해 **속성을 깨뜨립니다.**
HTML 속성 이스케이프를 쓰세요. `08-cutscenes.js`의 `esc()` 참고.

### 5. 컷신 성능 규칙

컷신은 합성 레이어가 많아 저사양 기기에서 쉽게 끊깁니다. 다음을 지키세요.

- `mix-blend-mode`는 **요소마다 걸지 말고** 컨테이너 한 장에 걸 것 (EQUINOX의 `.dfx` 참고)
- `filter`를 애니메이션하지 말 것. 매 프레임 재래스터가 발생합니다.
  대신 `opacity`/`transform`만 움직이는 별도 광채 레이어를 겹치세요.
- `clip-path` 애니메이션도 피하세요. 셔터 두 장을 `transform`으로 미는 방식이 훨씬 쌉니다.
- 회전·확대하는 큰 레이어에는 `will-change: transform`을 붙이세요.
- 요소 개수는 `QC(n)` 으로 감싸 품질 설정에 연동하세요.

### 6. 확인 없이 단정하지 말 것

이 프로젝트에서 잘못 짚은 진단이 여러 번 있었습니다.
증상만 보고 원인을 추정하지 말고, 계산하거나 실제로 렌더해서 확인한 뒤 고치세요.

- 무한 루프였던 것을 렌더링 부하로 오진한 적이 있습니다.
- 사슬 방향이 뒤집힌 것은 삼각함수로 계산해서야 확인됐습니다.
- 별 모양은 여러 후보를 실제로 래스터화해 비교한 뒤 골랐습니다.

## 밸런스 설계 원칙

수치를 고칠 때는 반드시 **전체 곡선을 계산**하고 바꾸세요. 한 항목만 보면 반드시 어긋납니다.

- **등급별 주화는 확률에 맞춰 올라가야 합니다.** 배율을 일정하게 두면 상위 등급의
  수입 기여도가 떨어져 "상위 검을 뽑을수록 손해"인 구조가 됩니다.
- **주화 배수는 합산으로 터집니다.** 탐욕의 저울, 도전과제, 검 고유효과, 황금 향로,
  중복 보너스, 환생 배수가 전부 곱해집니다. 한 항목을 올리기 전에 최대 누적을 계산하세요.
- **환생은 요구치가 지수로 늘어납니다.** 보상도 지수여야 회차별 도달 시간이 수렴합니다.
  선형 보상이면 후반이 반드시 막힙니다.

현재 기준값:
- 주화 배수 최대 누적 약 34배 (중복 포함)
- 환생 회차별 도달 시간 18~38분으로 수렴
- 최고 등급(운명) 1회 = 800만 주화

## 저장과 계정

- 저장은 `localStorage` (아티팩트 환경에서는 `window.storage`). `04-state.js`의 `store` 참고.
- 계정은 **기기 안의 저장 슬롯**일 뿐 진짜 인증이 아닙니다. 서버가 없습니다.
  비밀번호는 해시로 저장되지만 같은 기기에서 열람 가능합니다. 보안 장치로 취급하지 마세요.
- 이름이 `개발자`인 계정은 자동으로 개발자 권한을 가집니다.
- 권한과 계정 이름은 **저장 데이터가 아니라 계정 레지스트리가 기준**입니다.
  `load()`가 S를 덮어쓰므로 반드시 `load()` 이후에 `applyIdentity()`로 확정하세요.

## 저작권

레퍼런스 영상에서 **구조와 수치만** 참고했고 문양·자막·음원은 전부 새로 만들었습니다.
상용 음원이나 타 게임의 아트를 코드에 포함하지 마세요.
배경음악을 교체하려면 `06-audio.js`의 `EQ_BGM`에 본인이 라이선스를 가진 파일 경로를 넣으세요.

## 로컬 실행

서비스 워커와 홈 화면 설치는 HTTPS(또는 localhost)에서만 동작합니다.
`file://`로 열면 게임은 돌아가지만 PWA 기능은 비활성화됩니다.

```bash
python3 -m http.server 8000
# http://localhost:8000
```

## 배포

정적 파일이므로 Netlify Drop, Cloudflare Pages, GitHub Pages 어디든 폴더째 올리면 됩니다.
zip으로 올릴 때는 `index.html`이 **압축 파일 최상위**에 오게 하세요.
